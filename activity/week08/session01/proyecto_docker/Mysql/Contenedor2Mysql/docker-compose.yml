version: '3.8'
services:
  mysql:
    container_name: Contenedor2Mysql
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: 1234
    ports:
      - "3308:3306"
volumes:
  mysql_data:
    driver: local
