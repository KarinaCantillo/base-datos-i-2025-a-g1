version: '3.8'
services:
  db:
    container_name: contenedor2Postgres
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    environment:
      POSTGRES_PASSWORD: 123456
    ports:
      - "5434:5432"